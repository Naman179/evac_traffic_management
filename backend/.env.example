# EVAC Congestion System Configuration Example

# Backend Settings
APP_NAME=EVAC Congestion System API
APP_VERSION=1.0.0
DEBUG=false
HOST=0.0.0.0
PORT=8000

# Paths (Absolute paths or relative to backend directory)
# Leave blank to use default workspace structure:
# MODEL_DIR=../EVAC_Congestion_System-main (1)/EVAC_Congestion_System-main/ml/models
# DATA_DIR=../EVAC_Congestion_System-main (1)/EVAC_Congestion_System-main/data

# CORS Origins (Comma-separated or JSON list)
CORS_ORIGINS=["http://localhost:5173", "http://localhost:3000", "http://localhost"]
